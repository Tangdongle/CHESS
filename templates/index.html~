$def with (res_cats=[])

<div class="index_header">
    <h1>Database Categories By Title</h1>
    <div id="accordion">
        $for c in res_cats:
            <h2 class="category_name $loop.parity">$c</h2>
            <div>
            $for l in xrange(26):
                    <a href="/category/$c.replace(' ','_')/$chr(l+65)">$chr(l+65)</a>
            </div>
    </div>
</div>
$var title: Peer Saferety!
$var no_header: True
